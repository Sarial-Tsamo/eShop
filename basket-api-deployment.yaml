---
apiVersion: v1
kind: Service
metadata:
  name: basket-api
  namespace: eshop
  labels:
    app: basket-api
spec:
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: basket-api
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: basket-api
  namespace: eshop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: basket-api
  template:
    metadata:
      labels:
        app: basket-api
    spec:
      containers:
        - name: basket-api
          image: sarialbebeto/basket-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: ASPNETCORE_URLS
              value: "http://+:8080"
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ConnectionStrings__redis
              value: "redis:6379"
            - name: RabbitMQ__Host
              value: "eventbus"
            - name: Identity__Url
              value: "http://identity-api"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
