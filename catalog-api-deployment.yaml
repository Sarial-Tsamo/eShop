---
apiVersion: v1
kind: Service
metadata:
  name: catalog-api
  namespace: eshop
  labels:
    app: catalog-api
spec:
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: catalog-api
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-api
  namespace: eshop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-api
  template:
    metadata:
      labels:
        app: catalog-api
    spec:
      containers:
        - name: catalog-api
          image: sarialbebeto/catalog-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: ASPNETCORE_URLS
              value: "http://+:8080"
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ConnectionStrings__CatalogDb
              value: "Host=postgres;Port=5432;Database=catalogdb;Username=postgres;Password=postgres;"
            - name: RabbitMQ__Host
              value: "eventbus"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
