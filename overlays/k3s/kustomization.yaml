apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: eshop-k3s
  namespace: eshop

bases:
  - ../

patchesStrategicMerge:
  - webapp-patch.yaml
  - traefik-ingress.yaml

commonAnnotations:
  deployment-platform: k3s
  cluster-type: lightweight
