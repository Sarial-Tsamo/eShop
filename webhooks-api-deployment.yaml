---
apiVersion: v1
kind: Service
metadata:
  name: webhooks-api
  namespace: eshop
  labels:
    app: webhooks-api
spec:
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: webhooks-api
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: webhooks-api
  namespace: eshop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webhooks-api
  template:
    metadata:
      labels:
        app: webhooks-api
    spec:
      containers:
        - name: webhooks-api
          image: sarialbebeto/webhooks-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: ASPNETCORE_URLS
              value: "http://+:8080"
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ConnectionStrings__WebhooksDb
              value: "Host=postgres;Port=5432;Database=webhooksdb;Username=postgres;Password=postgres;"
            - name: RabbitMQ__Host
              value: "eventbus"
            - name: Identity__Url
              value: "http://identity-api"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
